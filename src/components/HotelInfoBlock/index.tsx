import * as React from 'react';
import * as Styled from './styles';

export interface HotelInfoBlockProps {}

const HotelInfoBlock: React.FC<HotelInfoBlockProps> = (props) => {
    return <Styled.Container>"內容開始"</Styled.Container>;
};

export default HotelInfoBlock;
